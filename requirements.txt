# Core (required)
numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.2
torch>=2.0

# Imbalance handling (MLP-SMOTE)
imbalanced-learn>=0.11

# Interpretability (attention vs SHAP concordance)
shap>=0.43

# Optional baselines (the core pipeline runs without these)
xgboost>=1.7
lightgbm>=4.0
pytorch-tabnet>=4.1
# rtdl>=0.0.13          # only if you enable the FT-Transformer baseline
